# =============================================================================
# 로컬 개발용 .env 예시
# 이 파일을 복사해서 .env 로 저장한 뒤 실제 값으로 채워주세요.
# =============================================================================

# Gemini API (필수 - 채팅/이미지 생성)
# https://aistudio.google.com/apikey 에서 발급
REACT_APP_GEMINI_API_KEY=your_gemini_api_key

# MongoDB Atlas 연결 문자열 (필수)
# 형식: mongodb+srv://USER:PASSWORD@CLUSTER.mongodb.net/
REACT_APP_MONGODB_URI=your_mongodb_connection_string

# YouTube Data API 키 (채널 다운로드용)
# https://console.cloud.google.com/ → APIs → YouTube Data API v3 → Credentials
YOUTUBE_API_KEY=your_youtube_api_key

# -----------------------------------------------------------------------------
# API URL (로컬 개발 시)
# -----------------------------------------------------------------------------
# 로컬에서는 비워두면 src/config/apiBase.js의 DEV_API_BASE 사용
# 또는 명시적으로 설정:
# REACT_APP_API_URL=http://localhost:3001

# -----------------------------------------------------------------------------
# Render 배포용 (프론트엔드 Static Site 환경 변수에 설정)
# -----------------------------------------------------------------------------
# REACT_APP_API_URL=https://chatapp-backend.onrender.com
# ↑ Render 대시보드 → chatapp-frontend → Environment 에서 설정
# ↑ 백엔드 Web Service의 실제 URL로 교체 (끝에 / 없이)

# -----------------------------------------------------------------------------
# Render 백엔드용 (Web Service 환경 변수)
# -----------------------------------------------------------------------------
# MONGODB_URI=your_mongodb_connection_string
# REACT_APP_GEMINI_API_KEY=your_gemini_api_key
# YOUTUBE_API_KEY=your_youtube_api_key
# RENDER_EXTERNAL_URL 은 Render가 자동 설정 (이미지 URL 생성에 사용)
